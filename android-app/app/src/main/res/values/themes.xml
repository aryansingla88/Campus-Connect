<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme (do not use directly in manifest) -->
    <style name="Base.Theme.CampusConnect"
        parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/primary</item>

        <item name="android:colorBackground">
            @color/background
        </item>

        <item name="colorSurface">
            @color/surface
        </item>

        <item name="colorOnSurface">
            @color/on_surface
        </item>

    </style>

    <!-- This is the actual app theme -->
    <style name="Theme.CampusConnect"
        parent="Base.Theme.CampusConnect" />

    <style name="MenuTextWhite">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>